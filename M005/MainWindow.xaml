<Window x:Class="M005.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:M005"
        Title="MainWindow" Height="450" Width="800"
		Topmost="True" WindowStartupLocation="CenterScreen"
		DataContext="{Binding RelativeSource={RelativeSource Self}}">
	<StackPanel>
		<!--Data Binding-->
		<!--Binding zw. einer GUI Komponente und dem Backend-->

		<!--Benötigte Komponente-->
		<!--DataContext, INotifyPropertyChanged, ObservableCollection-->

		<!--..................................................................................................-->
		
		<!--DataContext-->
		<!--Objekt, auf welches zugegriffen wird, wenn ein Binding ohne ElementName durchgeführt wird-->
		<!--Wird generell im Window gesetzt-->
		<!--Properties, welche gebunden werden sollen, müssen mit { get; set; } definiert werden-->
		<!--WICHTIG: DataContext wird immer an Unterkomponenten vererbt-->
		
		<!--DataContext setzen: -->
		<!--Frontend: DataContext="{Binding RelativeSource={RelativeSource Self}}"-->
		<!--Backend: this.DataContext = this;-->
		<TextBlock Text="{Binding Path=Counter}" FontSize="40"/>
		<Button Click="Button_Click" Content="Counter++"/>

		<!--..................................................................................................-->
		
		<!--INotifyPropertyChanged-->
		<!--Benachrichtigungsmechanismus-->
		<!--Muss immer im Backend implementiert werden-->

		<!--..................................................................................................-->
		
		<!--ObservableCollection-->
		<!--Liste, welche die Daten in der GUI automatisch anzeigt-->
		<!--WICHTIG: Diese Collection sollte niemals neu zugewiesen werden-->
		<!--Immer .Clear() benutzen und neu befüllen-->
		<ListBox ItemsSource="{Binding Zahlen}"/>
	</StackPanel>
</Window>