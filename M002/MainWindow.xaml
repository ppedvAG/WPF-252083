<Window x:Class="M002.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="450" Width="800"
		Topmost="True"
		WindowStartupLocation="CenterScreen">
	<StackPanel>
		<!--Content: Beliebige Objekte-->
		<Label Content=""/>

		<!--Text: String-->
		<TextBlock Text=""/>

		<Image Source=".\ppedv-Logo.png"/>
		<Image Source="C:\Users\lk3\Downloads\WPF_2025_11_25\M002\ppedv-Logo.png" Name="Img" Height="100"/>

		<!--Name eingeben -> F12-->
		<Button Click="DasIstEinButton" Content="Hallo"/>

		<TextBox Name="Eingabe"/>

		<!--Ohne ItemsSource-->
		<ComboBox>
			<ComboBoxItem Content="Eins"/>
			<ComboBoxItem Content="Zwei"/>
			<ComboBoxItem Content="Drei"/>
		</ComboBox>

		<!--ItemsSource: Siehe Backend Konstruktor-->
		<ComboBox Name="Wochentage"/>

		<CheckBox IsChecked="True"/>
		<RadioButton GroupName="Radio"/>
		<RadioButton GroupName="Radio"/>
		<RadioButton GroupName="Radio"/>
		<ToggleButton Content="Toggle"/>

		<Slider Minimum="0" Maximum="100" Value="50"
				TickFrequency="1" TickPlacement="BottomRight" IsSnapToTickEnabled="True"
				Foreground="Black"/>

		<Slider Minimum="0" Maximum="100" Value="50"
				Ticks="1, 2, 5, 10, 20, 50, 100, 200, 500" TickPlacement="BottomRight"
				Foreground="Black"/>

		<ProgressBar Minimum="0" Maximum="100" Value="50" Height="30"
					 IsIndeterminate="True">
			<ProgressBar.Foreground>
				<LinearGradientBrush>
					<GradientStop Offset="0" Color="Red"></GradientStop>
					<GradientStop Offset="1" Color="Green"></GradientStop>
				</LinearGradientBrush>
			</ProgressBar.Foreground>
		</ProgressBar>

		<Menu>
			<MenuItem Header="Datei">
				<MenuItem Header="Neu"/>
				<MenuItem Header="Öffnen">
					<MenuItem Header="Projekt" Click="ProjektClicked"/>
					<MenuItem Header="Datei"/>
					<MenuItem Header="..."/>
				</MenuItem>
				<MenuItem Header="Speichern"/>
				<MenuItem Header="Beenden"/>
			</MenuItem>
		</Menu>

		<Separator Margin="20"/>

		<Button Content="Test"/>
	</StackPanel>
</Window>